var $p = $('p');
console.log($p.text());

$form = $('form');
var key;

$(function() {
  console.log("YES");
  // $form.on('submit', function(e) {
  // e.preventDefault();
  // key = $form.find([input='text']).val().charCodeAt(0);
  // });
  
  // $(document).off('keypress').on('keypress', function(e) {
  //   if (e.which !== key) {
  //     return;
  //   } else {
      
  //   }
  // })
});
